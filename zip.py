#!/usr/bin/env python
from zipfile import *
def main(): #{
    file_name="shipping.zip";
    zip_archive=zipFile(file_name,"w");
    zip_archive.close();
    #}
    if(_name_=="_main_"):#{
        main();
        #}
